import 'package:flutter/material.dart';

import 'app_theme.dart';

/// The default light theme.
///
/// Equivalent to `AppTheme.light()`. Use [AppTheme.light] directly when you
/// want to pass a brand color, a font family or your own [ColorScheme].
ThemeData get lightTheme => AppTheme.light();
