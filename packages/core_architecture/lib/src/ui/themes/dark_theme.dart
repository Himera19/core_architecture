import 'package:flutter/material.dart';

import 'app_theme.dart';

/// The default dark theme.
///
/// Equivalent to `AppTheme.dark()`. Use [AppTheme.dark] directly when you
/// want to pass a brand color, a font family or your own [ColorScheme].
ThemeData get darkTheme => AppTheme.dark();
